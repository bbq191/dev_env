[user]
  email = 377810+bbq191@users.noreply.github.com
  name = Vinci Xu

[gpg]
  format = ssh

[core]
  excludesfile = "$Env:XDG_CACHE_HOME\\git\\gitignore_global"
  editor = nvim
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true    # use n and N to move between diff sections
  side-by-side = true
  # delta detects terminal colors automatically; set one of these to disable auto-detection
  # dark = true
  # light = true

[merge]
  conflictstyle = diff3

[diff]
  colorMoved = default
[pull]
  rebase = false
[url "ssh://git@ssh.github.com:443/"]
	insteadOf = git@github.com:
